"""Example MCP servers used throughout the deep dive.

Each module here is a standalone MCP server built with the official SDK's
high-level `FastMCP` API. They are deliberately named `servers/` (not `mcp/`):
naming a local package `mcp` would shadow the installed `mcp` SDK and break
every import in this repo.
"""
